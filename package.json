{
  "name": "fresh-finder",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "web:dev": "yarn workspace web dev",
    "web:build": "yarn workspace web build",
    "web:all-fix": "yarn workspace web lint:fix & yarn workspace web format:fix"
  }
}
